# This file is a marker for Python <= 3.2 REGULAR PACKAGES
# Importing the current package will automatically execute this file and all the other __init__.py files in the
# package hierarchy.

# The classes, functions and variables defined here will be defined directly in the package root instead of a module
# inside the package.
